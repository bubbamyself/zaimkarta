# ZaimKarta: MVP security checklist перед production

Документ фиксирует минимальный практический набор мер, после которого ZaimKarta можно выпускать на VPS как MVP. Это не “идеальная корпоративная безопасность”, а разумная защита для первого публичного запуска: закрыть очевидные дыры, не оставить базу и админку наружу, снизить риск ботов, накрутки кликов, потери данных и поломки сервера.

## Краткий вердикт

Запускаться можно после выполнения обязательного checklist из этого документа.

Сейчас проект уже имеет часть правильной базы:

- админская сессия подписывается через `ADMIN_SESSION_SECRET`;
- cookie админки `httpOnly`, `sameSite=lax`, `secure` в production;
- страницы админки помечены как `noindex`;
- CPA-переход `/go/[slug]` делает `302`, проверяет активность оффера, HTTPS CPA-ссылку и региональное ограничение;
- IP для лида хранится как hash, а не открытым текстом;
- загрузка логотипов ограничена SVG, размером 400 КБ, серверной проверкой и безопасным именем файла.

Но перед production обязательно нужно закрыть следующие MVP-риски:

- in-memory rate limit `/go/[slug]` нужно продублировать на reverse proxy и проверить на production;
- локальный fallback `LEAD_IP_HASH_SALT` остается только для development; в production он запрещен кодом, но нужен реальный секрет на VPS;
- нет общей проверки production env на `localhost`, слабые секреты и demo CPA-ссылки;
- схема production-бэкапов уже настроена и проверена, но нужно подтвердить первый автоматический cron-запуск;
- внешний uptime и server health-check настроены; пять HTTP-мониторов и реальный alert/recovery email проверены.

## Оценка текущего research-документа

`zaimkarta-prod-infra-security-research.md` полезен как широкий справочник: там правильно описаны VPS + Docker + Caddy/Nginx, закрытый PostgreSQL, HTTPS, бэкапы, мониторинг, SEO и аварийные сценарии.

Проблема документа для MVP: он слишком широкий. В нем смешаны обязательные меры перед первым релизом и улучшения, которые можно делать позже: staging, WAL/PITR, Prometheus/Grafana, полноценный централизованный логинг, отдельный managed PostgreSQL, WAF/CDN, подробный DR-процесс.

Практическое решение: использовать research как базу знаний, а перед релизом идти по этому MVP-checklist. Если пункт помечен как `обязательно`, без него публичный запуск лучше не делать.

## Что обязательно для MVP

- VPS с закрытым SSH по паролю, отдельным deploy user, firewall и fail2ban.
- Наружу открыты только `80`, `443` и SSH-порт.
- PostgreSQL не доступен из интернета.
- Docker-контейнеры не публикуют лишние порты наружу.
- Caddy или Nginx принимает весь внешний трафик, выдает HTTPS и проксирует в Next.js.
- Production env без `localhost`, demo-секретов и demo CPA-ссылок.
- Сильные `ADMIN_SESSION_SECRET` и `LEAD_IP_HASH_SALT`.
- Secure cookies включены.
- `/admin`, `/api`, `/go` закрыты от индексации.
- Security headers включены хотя бы на базовом уровне.
- Админка имеет сложный пароль и rate limit на login.
- `/go/[slug]` имеет rate limit и базовый bot filter.
- Загрузка логотипов ограничена по размеру, MIME/type, SVG-содержимому и папке хранения.
- Ежедневный `pg_dump` хранится вне VPS, восстановление хотя бы один раз проверено.
- Uptime alert, disk alert, CPU/RAM alert, error alert и канал связи для аварий.

## Что можно позже

- Отдельный staging-сервер.
- Managed PostgreSQL.
- WAL/PITR вместо обычных ежедневных дампов.
- Полноценный WAF/CDN.
- Prometheus + Grafana + Loki.
- 2FA в админке.
- IP allowlist для админки, если работа будет только с фиксированных IP.
- Отдельный admin-поддомен.
- Детальный аудит действий админов.
- Автоматическое распознавание фрода по кликам.

## Checklist перед релизом

### Этап 1: production env и секреты

- [x] Подготовлен безопасный шаблон `site/production.env.example` без настоящих секретов.
- [x] Настоящие `.env` и `production.env` закрыты правилами Git, example-файлы разрешены.
- [x] Создана понятная инструкция `docs/production-env-and-secrets.md` по созданию, хранению и замене секретов.
- [x] `ADMIN_SESSION_SECRET` и `LEAD_IP_HASH_SALT` обязательны в production; короткие и очевидно тестовые значения отклоняются.
- [x] Production-сборка проверена с тестовыми сильными секретами.

### Этап 6А: локальная подготовка production-инфраструктуры VPS

- [x] Dev-файл `docker-compose.yml` сохранен без изменений и продолжает поднимать только локальный PostgreSQL.
- [x] Подготовлен отдельный `deploy/docker-compose.prod.example.yml` для app, PostgreSQL и Caddy.
- [x] Подготовлен `deploy/Caddyfile.example` с HTTPS, основным доменом, `www`-редиректом, безопасной передачей IP, лимитом body и логами.
- [x] PostgreSQL не публикует порт `5432` наружу и находится в закрытой внутренней Docker-сети.
- [x] Next.js app не публикует порт `3000` наружу и доступен только reverse proxy.
- [x] Для `public/uploads/logos` предусмотрен persistent Docker-volume; внешний бэкап остается задачей 6Б.
- [x] Подготовлен `deploy/production.env.server.example` без настоящих секретов.
- [x] Подготовлен понятный план и checklist покупки VPS в `docs/vps-production-infra-plan.md`.
- [ ] На этапе 6Б настроить и с внешнего компьютера проверить firewall, SSH по ключу и fail2ban.
- [ ] На этапе 6Б выбрать дополнительный proxy/provider rate limit для `/go/`: стандартный Caddy без стороннего модуля его не обеспечивает.
- [x] На этапе 6Б настроена ротация Docker-логов и внешний backup-контур.
- [x] Server health-check установлен на VPS, ручной `all` и `/api/health/server` проверены.
- [x] Подключен бесплатный HTTP health monitor, реальный alert/recovery email проверен.

### Стоп-лист перед запуском на VPS

> Сейчас эти пункты намеренно не выполнены, потому что VPS еще не куплен. Первый публичный запуск запрещен, пока все четыре пункта ниже не отмечены.

- [ ] Сгенерирован настоящий уникальный `ADMIN_SESSION_SECRET` и сохранен в менеджере паролей и на VPS.
- [ ] Сгенерирован отдельный уникальный `LEAD_IP_HASH_SALT` и сохранен в менеджере паролей и на VPS.
- [ ] Сгенерирован отдельный уникальный `POSTGRES_PASSWORD` и сохранен в менеджере паролей и на VPS.
- [ ] Настоящий `POSTGRES_PASSWORD` подставлен в `DATABASE_URL`, после чего подключение сайта к production-базе проверено.

### Стоп-лист этапа 2: админка перед production

> Код базовой защиты готов, но эти проверки можно выполнить только с настоящей production-конфигурацией. Не считать защиту админки полностью принятой, пока все пункты ниже не отмечены.

- [ ] Для владельца создан длинный уникальный пароль админки и сохранен в менеджере паролей.
- [ ] В production-базе оставлены только нужные admin-пользователи; тестовые отключены или удалены.
- [ ] На production вручную проверено: неправильный логин и неправильный пароль показывают одинаковую ошибку.
- [ ] На production вручную сделано 9 неправильных попыток: вход временно блокируется, показывается время ожидания, в серверном логе есть warning без пароля.
- [ ] После окончания блокировки правильный логин снова работает, а logout действительно закрывает доступ к `/admin`.
- [ ] Reverse proxy перезаписывает `X-Forwarded-For`/`X-Real-IP`, и приложение получает реальный IP посетителя, а не произвольный заголовок от клиента.
- [ ] Подтверждено, что приложение запущено одним экземпляром. Если экземпляров несколько, in-memory rate limit нужно заменить согласованным общим ограничением или дополнить rate limit на reverse proxy.
- [ ] Принято явное решение по IP allowlist: включить только при постоянном IP; при мобильном или динамическом IP оставить выключенным.
- [ ] 2FA не забыта: она остается отдельным этапом после запуска базовой защиты, а не считается выполненной сейчас.

- [ ] `NODE_ENV=production`.
- [ ] `NEXT_PUBLIC_SITE_URL=https://zaimkarta.ru`.
- [ ] `DATABASE_URL` указывает на production PostgreSQL, не на локальную базу.
- [ ] `ADMIN_SESSION_SECRET` сгенерирован заново, длинный, уникальный, не хранится в репозитории.
- [ ] `LEAD_IP_HASH_SALT` сгенерирован заново, длинный, уникальный, не хранится в репозитории.
- [ ] В production запрещен fallback `zaimkarta-local` для `LEAD_IP_HASH_SALT`.
- [ ] В `.env`, HTML, canonical, JSON-LD и sitemap нет `localhost`.
- [ ] В базе нет активных demo CPA-ссылок на `example.com` или тестовые URL.
- [ ] Все активные CPA-ссылки начинаются с `https://`.
- [ ] В репозитории нет `.env`, паролей, токенов, приватных ключей и секретов CPA-сетей.
- [ ] GitHub Actions или локально проходят `npm run lint` и `npm run build`.
- [ ] Prisma migrations применены через production-safe команду миграций.
- [ ] Создан только нужный admin-пользователь, лишние тестовые удалены или отключены.
- [ ] Проверен ручной сценарий: главная, SEO-страница, оффер, выбор региона, CTA через `/go/[slug]`, запись клика, запрет перехода при региональном ограничении.

## Checklist настройки VPS

- [ ] Создан отдельный пользователь `deploy`, работа под `root` отключена.
- [ ] SSH-вход только по ключу.
- [ ] `PasswordAuthentication no`.
- [ ] `PermitRootLogin no`.
- [ ] SSH-порт известен владельцу и не конфликтует с firewall.
- [ ] Установлен firewall: разрешены только `80/tcp`, `443/tcp` и SSH.
- [ ] PostgreSQL-порт `5432` не открыт наружу.
- [ ] Docker не публикует наружу порт приложения и базы; наружу смотрит только reverse proxy.
- [ ] Установлен fail2ban для SSH и, при возможности, для Nginx/Caddy.
- [ ] Включены автоматические security updates.
- [ ] Включена ротация логов, чтобы диск не забился логами.
- [ ] У хостера включены базовые DDoS-фильтры или выбран тариф/провайдер с такой защитой.
- [ ] Есть доступ к emergency console у провайдера на случай ошибки SSH/firewall.

## Checklist reverse proxy и HTTPS

- [ ] Используется Caddy или Nginx как единая входная точка.
- [ ] `http://zaimkarta.ru` редиректит на `https://zaimkarta.ru`.
- [ ] `https://www.zaimkarta.ru` редиректит на `https://zaimkarta.ru`.
- [ ] TLS-сертификат получен и автообновляется.
- [ ] Включено gzip/brotli-сжатие.
- [ ] Настроены лимиты размера запроса, чтобы нельзя было заливать большие файлы.
- [ ] Для `/admin/login` и `/go/` на уровне proxy добавлен базовый rate limit, если он еще не реализован в приложении.
- [ ] В access log пишутся IP, URL, статус, user-agent и время ответа.

## Checklist защиты приложения

- [ ] В production нет stack traces для пользователя.
- [ ] `NEXT_PUBLIC_*` переменные не содержат секретов.
- [x] `ADMIN_SESSION_SECRET` обязателен: без него админка не работает в production.
- [x] `LEAD_IP_HASH_SALT` обязателен в production; локальная соль там отклоняется.
- [x] Cookie админки имеет `httpOnly`, `secure` в production и `sameSite=lax`. Lead-cookie проверяется отдельно на соответствующем этапе.
- [x] Sitemap содержит только публичные маршруты; `/admin/*`, `/api/*` и `/go/*` в него не попадают.
- [x] `/admin/*`, `/api/*`, `/go/*` закрыты в `robots.txt`.
- [x] Для admin-страниц есть `robots: { index: false, follow: false }`.
- [x] Для `/go/[slug]` и `/api/offers/[slug]/click` добавлен `X-Robots-Tag: noindex, nofollow`.
- [x] Включены базовые security headers: `Strict-Transport-Security`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy`, `X-Frame-Options`.
- [ ] Для публичных партнерских ссылок используется `/go/[slug]`, прямые CPA-ссылки не светятся в HTML.
- [ ] Ошибки БД и внутренние ошибки не возвращаются пользователю текстом.

## Checklist защиты админки

- [ ] Пароль администратора длинный, уникальный, хранится в менеджере паролей.
- [ ] Нет admin-пользователей с demo-паролями.
- [x] При ошибке логина показывается одинаковый текст: “Неверный логин или пароль”.
- [x] Вход ограничен независимо по IP и нормализованному username: 8 попыток за 15 минут. Реализация in-memory подходит для одного процесса MVP, но сбрасывается при перезапуске и не синхронизируется между несколькими экземплярами приложения.
- [x] При превышении лимита сервер пишет минимальный warning с IP, username и временем ожидания; пароль и другие данные в лог не попадают.
- [x] Cookie сессии `httpOnly`, `secure` в production, `sameSite=lax`, path `/admin`.
- [x] Время жизни сессии ограничено 12 часами.
- [x] Logout очищает cookie текущего пользователя и переводит на страницу входа.
- [x] Все проверенные admin pages и actions требуют активную серверную сессию; изменяющие действия дополнительно ограничены ролью или назначенным правом в принятой модели доступа.
- [x] Экспорт аналитики доступен только `BOSS` или администратору с правом `analytics`.
- [x] Админка закрыта от индексации через page metadata, `X-Robots-Tag` и `robots.txt`.
- [x] IP allowlist отложен до покупки VPS: он полезен при 1-2 постоянных IP, но при мобильном или динамическом интернете может закрыть владельцу доступ.
- [x] 2FA намеренно вынесена в отдельный этап после базовой защиты админки.

## Checklist защиты `/go/[slug]` от накрутки

- [x] `/go/[slug]` возвращает `302`, не `301`.
- [x] CPA-секреты и полный tracking URL не показываются на публичной странице.
- [x] CPA redirect строится только на серве.
- [x] Проверяется, что оффер активен.
- [x] Проверяется, что affiliate offer активен.
- [x] Проверяется, что CPA-ссылка `https://`.
- [x] Сервер проверяет региональное ограничение перед редиректом.
- [x] Добавлены независимые in-memory rate limit по IP hash + slug и lead cookie + slug; при блокировке ответ имеет `429` и `Retry-After`.
- [x] Подозрительные user-agent, пустые user-agent и очевидные crawlers не отправляются в CPA, а получают fallback.
- [x] Поисковые боты не индексируют `/go/*`: есть `robots.txt` и `X-Robots-Tag`.
- [x] Подготовлен базовый alert по общему всплеску `/go` `429` в Caddy logs без хранения IP и query strings.
- [ ] После появления трафика добавить отдельные пороги по offer slug; агрегация по IP/user-agent намеренно отложена.
- [x] Неуспешные переходы пишут минимальные warning-логи: bot, offer unavailable, restricted region, invalid CPA URL, rate limited. Одинаковые warning по slug подавляются на минуту, чтобы бот не забил диск. IP, секреты и полные CPA URL не логируются.
- [ ] В отчетах для CPA можно отделить нормальные клики от отфильтрованных.

MVP-логика rate limit может быть простой: сначала на уровне Nginx/Caddy, затем в приложении. Для одного VPS достаточно лимита на route `/go/`: например, несколько десятков переходов в минуту на IP и более жесткий лимит на один slug. Точные числа лучше подстроить после первых реальных данных.

## Checklist защиты загрузки логотипов

- [x] Загрузка доступна только авторизованному `BOSS` или администратору с правом `offers_write`; право проверяет server action до чтения и записи файла.
- [x] Сервер ограничивает размер файла значением `400 КБ`; слишком большой файл не записывается. Браузер повторяет проверку только для удобства пользователя.
- [x] Сервер принимает только `image/svg+xml`; расширение `.svg` и уникальное имя с UUID задаются сервером, пользовательское имя не используется.
- [x] SVG проверяется на наличие `<svg>` и отсутствие `<script>`, inline-обработчиков, `javascript:`, `<foreignObject>`, doctype/entity и внешних HTTP/HTML-загрузок.
- [x] Файлы сохраняются только в `public/uploads/logos`; итоговый путь дополнительно проверяется перед записью.
- [x] `currentLogoUrl` принимается только как ранее сгенерированный локальный путь `/uploads/logos/<slug>-<uuid>.svg`; внешние URL, `..`, другие разделы сайта и protocol-relative URL отклоняются.
- [x] Для `/uploads/logos/*` включены `X-Content-Type-Options: nosniff`, ограничивающий CSP и длительный immutable-cache, безопасный благодаря UUID в имени.
- [x] Публичные карточки и страницы сохраняют fallback на `logoText`, если логотип не задан.
- [x] Старые логотипы автоматически не удаляются, чтобы случайно не удалить используемый файл.
- [ ] Позже предусмотреть безопасную уборку неиспользуемых старых логотипов.
- [ ] Если появятся сомнения по SVG, перейти на PNG/WebP или согласовать полноценный sanitizer. Текущая проверка — практичная защита MVP, а не универсальный XML/SVG sanitizer.

## Checklist бэкапов

- [x] Подготовлен безопасный скрипт `deploy/scripts/backup-production.sh` без секретов в Git.
- [x] Скрипт создаёт сжатый PostgreSQL dump и проверяет его через `pg_restore --list`.
- [x] Скрипт архивирует `uploads/logos` из Docker-volume без удаления рабочих логотипов.
- [x] В скрипте предусмотрен непубличный каталог `/home/deploy/backups/zaimkarta` с закрытыми правами.
- [x] В скрипте предусмотрено хранение минимум 7 ежедневных и 4 недельных копий.
- [x] Скрипт установлен и успешно запущен на production VPS.
- [x] Настроен ежедневный автоматический `pg_dump` production-базы и архивирование uploads.
- [x] Внешняя копия хранится на Google Drive в закрытой папке `ZaimKarta Production Backups` через `rclone`.
- [x] На VPS подтверждено, что бэкап не лежит в публичной папке сайта и не доступен по URL.
- [x] Бэкап сжимается; решение по шифрованию ещё не принято владельцем.
- [x] Cron настроен на `03:20 UTC`, ручной запуск и журнал проверены.
- [ ] После первого автоматического запуска проверить журнал cron и свежие файлы на Google Drive.
- [x] До релиза выполнено тестовое восстановление в изолированный контейнер и проверены 6 ключевых таблиц.
- [x] Инструкция создания, проверки, ручного скачивания и восстановления записана в `docs/production-backup-plan.md`.
- [x] Внешнее хранение и успешное тестовое восстановление подтверждены до закрытия этапа.

## Минимальный план защиты от DDoS и ботов

- [ ] Выбрать VPS-провайдера с базовой DDoS-защитой.
- [ ] Держать reverse proxy единственной публичной точкой входа.
- [ ] Закрыть все лишние порты.
- [ ] Включить rate limit на `/admin/login`.
- [ ] Включить rate limit на `/go/`.
- [ ] Включить общий лимит запросов на IP для слишком частых запросов к сайту.
- [ ] Ограничить размер body upload.
- [ ] Быстро блокировать IP/подсети через firewall при явной атаке.
- [ ] Иметь заранее подготовленный “режим защиты”: временно усилить лимиты `/go`, закрыть админку по IP allowlist, включить challenge/WAF у провайдера или CDN, если доступно.

Важно: MVP-защита не остановит большую DDoS-атаку уровня оператора связи. Ее задача другая: не дать простому боту, сканеру или накрутчику положить маленький VPS и не дать использовать сайт как бесплатный редирект-автомат.

## Monitoring и аварийная реакция

- [x] UptimeRobot проверяет главную страницу каждые 5 минут.
- [x] UptimeRobot каждые 5 минут проверяет `robots.txt`, `sitemap.xml` и `/admin/login`.
- [ ] Проверка `/go/[slug]` на тестовом/служебном оффере или безопасном fallback-сценарии.
- [x] Установлен alert по диску от 80% и по свободному месту в backup directory.
- [x] Установлен alert по RAM и высокой load average.
- [x] Установлен Docker health/restart alert для `app`, `db`, `caddy`.
- [x] Подготовлен базовый alert по `5xx` на Caddy; Sentry для MVP не добавляется.
- [x] Подготовлен alert по резкому росту `4xx` и `/go` `429` без создания CPA-кликов.
- [ ] После появления трафика настроить отдельные пороги по offer slug/IP/user-agent.
- [x] Создан `docs/production-incident-runbook.md`, включая режим `ТЕХРАБОТЫ` и восстановление.
- [x] Создан `docs/production-monitoring-plan.md` с командами, расписанием и ограничениями MVP.
- [x] Подтверждено тестовое письмо UptimeRobot на `chrk.omsk@gmail.com`; первые письма найдены в спаме и отмечены как «Не спам».
- [x] Health-check установлен на VPS; ручной `all`, backup check и ответ `/api/health/server` `200 ok` подтверждены.
- [x] `/api/health/server` подключен к UptimeRobot; реальный Down и последующий Recovery подтверждены.
- [x] Первый автоматический быстрый health cron подтвержден по сохранению ответа `200` после истечения 12 минут от ручного запуска.

### План действий при атаке или поломке

1. Проверить, жив ли VPS и сколько занято CPU/RAM/disk.
2. Посмотреть access log reverse proxy: много ли запросов, какие URL, IP и user-agent.
3. Если бьют `/go`, временно усилить rate limit и отдавать `429`.
4. Если бьют `/admin/login`, временно закрыть `/admin` по IP allowlist.
5. Если забивается диск, остановить рост логов, включить/исправить logrotate, удалить только безопасные старые логи.
6. Если приложение упало после деплоя, откатить контейнер/коммит и проверить миграции.
7. Если есть признаки взлома, изолировать сервер, сменить секреты, пароли, SSH-ключи и CPA-доступы, поднимать чистый сервер из проверенного бэкапа.

## Задачи до релиза

- [x] Добавить production-проверку, что `LEAD_IP_HASH_SALT` задан и не используется `zaimkarta-local`.
- [x] Добавить rate limit для `/admin/login`.
- [x] Добавить rate limit для `/go/[slug]`.
- [x] Добавить `X-Robots-Tag: noindex, nofollow` для `/go/*` и `/api/*`.
- [x] Добавить или настроить security headers.
- [x] Подготовить `robots.txt` и убедиться, что `/admin`, `/api`, `/go` закрыты.
- [x] Sitemap генерируется из главной, `/blog`, `/services`, опубликованных SEO-страниц и активных offer pages.
- [ ] Настроить VPS по checklist выше.
- [ ] Настроить HTTPS и редиректы домена.
- [ ] Настроить ежедневный `pg_dump` вне VPS и проверить восстановление.
- [x] Завершить resource alerts: бесплатный `/api/health/server` monitor и alert/recovery email проверены.
- [ ] Удалить или выключить demo CPA-ссылки.
- [ ] Создать сильного admin-пользователя и удалить тестовых.

## Задачи после релиза

- [ ] Первые 7 дней ежедневно смотреть логи ошибок, `/go`-клики, 404/429/5xx и загрузку сервера.
- [ ] Проверить индексацию в Яндекс Webmaster и Google Search Console.
- [ ] Проверить, что CPA-сети видят корректные `lead_id`, `click_id`, `sub1`, `sub2`, `sub3`.
- [ ] Проверить, что бэкапы продолжают создаваться каждый день.
- [ ] Добавить журнал входов в админку и важных admin-действий.
- [ ] Подумать об IP allowlist для админки, если станет понятно, откуда владелец обычно заходит.

## Задачи, когда появится трафик

- [ ] Настроить отдельные пороги аномалий по каждому offer slug.
- [ ] Добавить более умную антифрод-логику для `/go`: повторы, слишком быстрые клики, подозрительные user-agent, частые клики по разным офферам.
- [ ] Вынести PostgreSQL в managed DB или хотя бы на отдельный VPS, если база начнет конкурировать за ресурсы с приложением.
- [ ] Добавить WAF/CDN или защиту провайдера повышенного уровня.
- [ ] Настроить полноценные дашборды по кликам, ошибкам, latency, CPU/RAM/disk.
- [ ] Добавить staging-окружение перед крупными изменениями.
- [ ] Рассмотреть 2FA для админки.
- [ ] Перейти от ежедневных дампов к PITR/WAL, если база станет бизнес-критичной.
